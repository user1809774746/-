# 管理员登录接口文档

> **接口名称**：管理员登录认证  
> **接口版本**：v1.0  
> **文档日期**：2025-10-31

---

## 📡 接口基本信息

| 项目 | 内容 |
|------|------|
| **接口名称** | 管理员登录认证 |
| **接口路径** | `POST /api/auth/login` |
| **接口描述** | 管理员使用手机号和密码登录系统 |
| **Content-Type** | `application/json` |
| **认证方式** | 无需认证（登录接口本身不需要token） |

---

## 📥 请求参数

### 请求头 (Headers)

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| Content-Type | String | 是 | 必须为 `application/json` |

### 请求体 (Body)

| 参数名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| phone | String | 是 | 管理员手机号（11位） | "18888888888" |
| password | String | 是 | 登录密码（明文） | "123123" |
| userType | String | 是 | 用户类型（固定为"admin"） | "admin" |

### 参数详细说明

#### phone（手机号）
- **格式要求**：11位数字，符合中国大陆手机号格式
- **正则表达式**：`^1[3-9]\d{9}$`
- **说明**：必须是已注册的管理员手机号
- **示例**：`18888888888`

#### password（密码）
- **格式**：明文密码字符串
- **长度**：无限制（但注册时要求≥6位）
- **说明**：后端会使用BCrypt验证
- **示例**：`123123`

#### userType（用户类型）
- **固定值**：`"admin"`
- **说明**：用于区分普通用户和管理员登录
- **注意**：此参数必须传递，否则无法识别为管理员登录

---

## 📤 响应格式

### 成功响应（HTTP 200）

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9.eyJwaG9uZSI6IjE4ODg4ODg4ODg4IiwidXNlclR5cGUiOiJhZG1pbiIsImV4cCI6MTczMDQ0MzIwMH0.xxxxx",
    "userType": "admin",
    "phone": "18888888888"
  }
}
```

### 失败响应

#### 1. 手机号或密码错误（HTTP 200 with code 401）
```json
{
  "code": 401,
  "message": "手机号或密码错误",
  "data": null
}
```

#### 2. 参数缺失（HTTP 200 with code 400）
```json
{
  "code": 400,
  "message": "无效的用户类型",
  "data": null
}
```

#### 3. 手机号格式错误（HTTP 200 with code 400）
```json
{
  "code": 400,
  "message": "手机号格式不正确",
  "data": null
}
```

---

## 📋 响应字段说明

### 成功响应 data 字段

| 字段名 | 类型 | 说明 |
|--------|------|------|
| token | String | JWT token，用于后续请求认证（有效期7天） |
| userType | String | 用户类型（"admin"） |
| phone | String | 管理员手机号 |

### Token 说明

- **类型**：JWT (JSON Web Token)
- **有效期**：7天
- **用途**：访问需要认证的接口时，在请求头中携带
- **格式**：`Authorization: Bearer {token}`
- **包含信息**：
  - `phone`：管理员手机号
  - `userType`：用户类型（admin）
  - `exp`：过期时间戳

---

## 🧪 请求示例

### JavaScript (Fetch API)

```javascript
fetch('http://localhost:8081/api/auth/login', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    phone: "18888888888",
    password: "123123",
    userType: "admin"
  })
})
.then(response => response.json())
.then(data => {
  if (data.code === 200) {
    console.log('登录成功:', data.data);
    // 保存token到localStorage
    localStorage.setItem('token', data.data.token);
    localStorage.setItem('userType', data.data.userType);
    localStorage.setItem('phone', data.data.phone);
    
    // 跳转到管理员后台
    window.location.href = '/admin/dashboard.html';
  } else {
    console.error('登录失败:', data.message);
    alert('登录失败: ' + data.message);
  }
})
.catch(error => {
  console.error('请求失败:', error);
  alert('网络错误，请稍后重试');
});
```

### JavaScript (Axios)

```javascript
import axios from 'axios';

try {
  const response = await axios.post('http://localhost:8081/api/auth/login', {
    phone: "18888888888",
    password: "123123",
    userType: "admin"
  });
  
  if (response.data.code === 200) {
    const { token, userType, phone } = response.data.data;
    
    // 保存登录信息
    localStorage.setItem('token', token);
    localStorage.setItem('userType', userType);
    localStorage.setItem('phone', phone);
    
    // 跳转到管理员后台
    window.location.href = '/admin/dashboard.html';
  } else {
    alert('登录失败: ' + response.data.message);
  }
} catch (error) {
  console.error('登录失败:', error);
  alert('网络错误，请稍后重试');
}
```

### curl 命令

```bash
curl -X POST http://localhost:8081/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "18888888888",
    "password": "123123",
    "userType": "admin"
  }'
```

### Postman 配置

- **Method**: POST
- **URL**: `http://localhost:8081/api/auth/login`
- **Headers**:
  ```
  Content-Type: application/json
  ```
- **Body** (raw, JSON):
  ```json
  {
    "phone": "18888888888",
    "password": "123123",
    "userType": "admin"
  }
  ```

---

## 🔐 Token 使用方法

### 在后续请求中携带 Token

登录成功后，访问需要认证的接口时，需要在请求头中携带token：

#### JavaScript 示例

```javascript
// 从localStorage获取token
const token = localStorage.getItem('token');

// 方法1: Fetch API
fetch('http://localhost:8081/api/admin/posts/pending', {
  method: 'GET',
  headers: {
    'Authorization': 'Bearer ' + token
  }
})
.then(response => response.json())
.then(data => console.log(data));

// 方法2: Axios
axios.get('http://localhost:8081/api/admin/posts/pending', {
  headers: {
    'Authorization': 'Bearer ' + token
  }
})
.then(response => console.log(response.data));
```

#### curl 示例

```bash
curl -X GET http://localhost:8081/api/admin/posts/pending \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiJ9..."
```

---

## 🔄 完整的登录流程

### 前端登录流程

```
1. 用户输入手机号和密码
   ↓
2. 前端发送POST请求到 /api/auth/login
   Body: { phone, password, userType: "admin" }
   ↓
3. 后端验证手机号和密码
   ↓
4. 验证成功，返回token
   ↓
5. 前端保存token到localStorage
   ↓
6. 前端跳转到管理员后台
   ↓
7. 后续请求都携带token访问受保护的接口
```

### 后端验证流程

```
1. 接收登录请求
   ↓
2. 根据userType判断是管理员登录
   ↓
3. 根据phone查询administrator_info表
   ↓
4. 使用BCrypt验证密码
   - passwordEncoder.matches(输入密码, 数据库密码)
   ↓
5. 验证成功
   ↓
6. 生成JWT token（有效期7天）
   ↓
7. 保存token到数据库（active_token字段）
   ↓
8. 返回token给前端
```

---

## 🔍 密码验证说明

### BCrypt 密码验证

- **存储格式**：`$2a$10$dLxApnzILICY6oYmJ1anVuV7fcWkAX5hzeotVOA5tk6naDoBdVokW`
- **验证方法**：`passwordEncoder.matches(明文密码, BCrypt哈希)`
- **特点**：
  - 每次加密结果不同（包含随机盐）
  - 但同一明文都能验证成功
  - 安全性高，无法逆向破解

### 示例

```java
// 注册时加密
String password = "123123";
String encrypted = passwordEncoder.encode(password);
// 结果: $2a$10$dLxApn...

// 登录时验证
boolean matches = passwordEncoder.matches("123123", encrypted);
// 结果: true
```

---

## 🧪 测试用例

### 正常流程测试

| 测试项 | 输入 | 预期输出 |
|--------|------|----------|
| 正常登录 | phone: "18888888888"<br>password: "123123"<br>userType: "admin" | code: 200<br>返回token |

### 异常流程测试

| 测试项 | 输入 | 预期输出 |
|--------|------|----------|
| 手机号错误 | phone: "18999999999"<br>password: "123123"<br>userType: "admin" | code: 401<br>"手机号或密码错误" |
| 密码错误 | phone: "18888888888"<br>password: "wrong"<br>userType: "admin" | code: 401<br>"手机号或密码错误" |
| userType错误 | phone: "18888888888"<br>password: "123123"<br>userType: "user" | code: 401<br>"手机号或密码错误" |
| 缺少userType | phone: "18888888888"<br>password: "123123" | code: 400<br>"无效的用户类型" |

---

## 🎯 前端登录页面示例

### HTML + JavaScript 完整示例

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>管理员登录</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
        
        .login-container {
            background: white;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.2);
            width: 350px;
        }
        
        h2 {
            text-align: center;
            color: #333;
            margin-bottom: 30px;
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        label {
            display: block;
            margin-bottom: 5px;
            color: #555;
            font-weight: bold;
        }
        
        input {
            width: 100%;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 14px;
            box-sizing: border-box;
        }
        
        input:focus {
            outline: none;
            border-color: #667eea;
        }
        
        button {
            width: 100%;
            padding: 12px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            transition: transform 0.2s;
        }
        
        button:hover {
            transform: translateY(-2px);
        }
        
        button:active {
            transform: translateY(0);
        }
        
        .error-message {
            color: #dc3545;
            font-size: 14px;
            margin-top: 10px;
            text-align: center;
            display: none;
        }
        
        .loading {
            opacity: 0.6;
            cursor: not-allowed;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <h2>管理员登录</h2>
        <form id="loginForm">
            <div class="form-group">
                <label for="phone">手机号</label>
                <input type="text" id="phone" placeholder="请输入手机号" required>
            </div>
            
            <div class="form-group">
                <label for="password">密码</label>
                <input type="password" id="password" placeholder="请输入密码" required>
            </div>
            
            <button type="submit" id="loginBtn">登录</button>
            
            <div class="error-message" id="errorMessage"></div>
        </form>
    </div>

    <script>
        const API_BASE = 'http://localhost:8081';
        
        document.getElementById('loginForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            
            const phone = document.getElementById('phone').value.trim();
            const password = document.getElementById('password').value.trim();
            const loginBtn = document.getElementById('loginBtn');
            const errorMessage = document.getElementById('errorMessage');
            
            // 隐藏错误信息
            errorMessage.style.display = 'none';
            
            // 参数验证
            if (!/^1[3-9]\d{9}$/.test(phone)) {
                showError('手机号格式不正确');
                return;
            }
            
            if (password.length < 6) {
                showError('密码长度至少6位');
                return;
            }
            
            // 显示加载状态
            loginBtn.textContent = '登录中...';
            loginBtn.classList.add('loading');
            loginBtn.disabled = true;
            
            try {
                const response = await fetch(`${API_BASE}/api/auth/login`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({
                        phone: phone,
                        password: password,
                        userType: 'admin'
                    })
                });
                
                const data = await response.json();
                
                if (data.code === 200) {
                    // 登录成功
                    const { token, userType, phone } = data.data;
                    
                    // 保存到localStorage
                    localStorage.setItem('token', token);
                    localStorage.setItem('userType', userType);
                    localStorage.setItem('phone', phone);
                    
                    // 提示成功
                    loginBtn.textContent = '登录成功！';
                    loginBtn.style.background = '#28a745';
                    
                    // 延迟跳转
                    setTimeout(() => {
                        window.location.href = '/admin/dashboard.html';
                    }, 500);
                    
                } else {
                    // 登录失败
                    showError(data.message || '登录失败');
                    resetButton();
                }
                
            } catch (error) {
                console.error('登录失败:', error);
                showError('网络错误，请稍后重试');
                resetButton();
            }
        });
        
        function showError(message) {
            const errorMessage = document.getElementById('errorMessage');
            errorMessage.textContent = message;
            errorMessage.style.display = 'block';
        }
        
        function resetButton() {
            const loginBtn = document.getElementById('loginBtn');
            loginBtn.textContent = '登录';
            loginBtn.classList.remove('loading');
            loginBtn.disabled = false;
        }
    </script>
</body>
</html>
```

---

## 📊 接口状态码汇总

| HTTP状态码 | 业务状态码 | 说明 | 处理建议 |
|------------|------------|------|----------|
| 200 | 200 | 登录成功 | 保存token，跳转到管理员后台 |
| 200 | 401 | 手机号或密码错误 | 提示用户重新输入 |
| 200 | 400 | 参数错误 | 检查参数是否正确 |
| 404 | - | 接口不存在 | 检查URL路径 |
| 500 | 500 | 服务器错误 | 稍后重试或联系技术支持 |

---

## 🔗 相关接口

### 管理员注册接口
- **路径**：`POST /api/auth/admin/quick-register`
- **说明**：注册新的管理员账号

### 用户身份查询接口
- **路径**：`GET /api/auth/user-info`
- **说明**：查询当前登录用户的身份信息

### 帖子审核接口（需要token）
- **路径**：`GET /api/admin/posts/pending`
- **说明**：获取待审核帖子列表

---

## 🗄️ 数据库说明

### 管理员信息表

**表名**：`administrator_info`

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| adminid | BIGINT | 主键，自增 | 1 |
| admin_name | VARCHAR(255) | 管理员用户名 | "18888888888" |
| phone | VARCHAR(20) | 手机号（唯一） | "18888888888" |
| password | VARCHAR(255) | 密码（BCrypt加密） | "$2a$10$dLx..." |
| creation_date | DATETIME | 创建时间 | "2025-10-31 03:11:08" |
| last_login_date | DATETIME | 最后登录时间 | NULL |
| active_token | VARCHAR(500) | 当前活跃token | NULL |
| token_created_at | DATETIME | token创建时间 | NULL |
| token_expires_at | DATETIME | token过期时间 | NULL |

### 测试数据

```sql
-- 测试管理员账号
INSERT INTO administrator_info VALUES (
    1, 
    '18888888888', 
    '18888888888', 
    '$2a$10$dLxApnzILICY6oYmJ1anVuV7fcWkAX5hzeotVOA5tk6naDoBdVokW', 
    '2025-10-31 03:11:08', 
    NULL, NULL, NULL, NULL
);

-- 登录信息
-- 手机号: 18888888888
-- 密码: 123123
```

---

## ⚠️ 注意事项

### 安全建议

1. **HTTPS传输**：生产环境必须使用HTTPS传输密码
2. **Token保护**：Token不要暴露在URL中，应放在请求头
3. **Token刷新**：Token过期前应提醒用户刷新
4. **登录限制**：建议添加登录失败次数限制

### 常见问题

#### Q1: Token过期怎么办？
**A**: Token过期后会返回401错误，需要重新登录获取新token

#### Q2: 可以在多个设备同时登录吗？
**A**: 可以。但后登录的会覆盖之前的token（顶号机制）

#### Q3: 忘记密码怎么办？
**A**: 目前没有找回密码功能，需要联系其他管理员重置

#### Q4: 为什么要传userType参数？
**A**: 因为普通用户和管理员共用一个登录接口，用userType区分

---

## 📝 更新日志

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v1.0 | 2025-10-31 | 初始版本，基础登录功能 |

---

**文档结束**
